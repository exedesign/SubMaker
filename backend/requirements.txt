# SubMaker Backend Dependencies
# =============================

# Transcription (Faster-Whisper — CTranslate2 based, built-in word timestamps)
faster-whisper>=1.0.0

# Translation (Offline)
argostranslate>=1.10.0

# Local LLM Translation (Qwen2.5-3B-Instruct-AWQ)
transformers>=4.40.0
accelerate>=0.30.0
datasets  # Required by autoawq model loading
autoawq  # AWQ quantization backend (Windows: pip install autoawq --no-build-isolation --no-deps)

# Arabic Support (RTL)
arabic-reshaper>=3.0.0
python-bidi>=0.4.2

# API Framework
flask>=3.0.0
flask-cors>=4.0.0
flask-socketio>=5.3.0
eventlet>=0.35.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0

# Vocal Isolation (BS-Roformer HyperACE v2 + Resurrection UNWA via audio-separator)
audio-separator[gpu]>=0.24.0

# ONNX Runtime — GPU version only (do NOT install both onnxruntime and onnxruntime-gpu)
# pip install onnxruntime-gpu
# If you get "cudnnGetLibConfig" errors: pip uninstall onnxruntime; pip install onnxruntime-gpu

# ID3 Tag Writing
mutagen>=1.47.0

# Utilities
numpy>=1.24.0
tqdm>=4.65.0
pydub>=0.25.1

# GPU Support — PyTorch with CUDA (NVIDIA GPU acceleration)
# Install manually: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
# torch>=2.0.0  # Do NOT install via requirements.txt — use the CUDA index URL above
